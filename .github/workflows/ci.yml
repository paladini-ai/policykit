name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & Test (Node)
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18, 20, 22]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm turbo run build --filter='!@policy-kit/contracts'

      - name: Test
        run: pnpm turbo run test --filter='!@policy-kit/contracts'

  contracts:
    name: Contracts (Foundry)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: contracts

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1

      - name: Build contracts
        run: forge build

      - name: Run tests
        run: forge test -vvv

      - name: Run gas report
        run: forge test --gas-report

      - name: Run coverage
        run: forge coverage --ir-minimum

  slither:
    name: Slither Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write

    defaults:
      run:
        working-directory: contracts

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1

      - name: Build contracts
        run: forge build

      - name: Create SARIF output directory
        run: mkdir -p results
        working-directory: .

      - name: Run Slither
        uses: crytic/slither-action@v0.4.0
        id: slither
        continue-on-error: true
        with:
          target: contracts/
          slither-config: contracts/slither.config.json
          fail-on: medium
          sarif: results/slither.sarif

      - name: Upload Slither SARIF
        uses: github/codeql-action/upload-sarif@v4
        if: always() && hashFiles('results/slither.sarif') != ''
        with:
          sarif_file: results/slither.sarif

      - name: Fail on Slither findings
        if: steps.slither.outcome == 'failure'
        run: exit 1

  echidna:
    name: Echidna Fuzzing
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: contracts

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1

      - name: Build contracts with build-info
        run: forge build --build-info

      - name: Run Echidna – PolicyEngine
        uses: crytic/echidna-action@v2
        with:
          files: test/echidna/EchidnaPolicyEngine.sol
          contract: EchidnaPolicyEngine
          config: echidna.yaml
          echidna-workdir: contracts
          crytic-args: --ignore-compile

      - name: Run Echidna – MaxValueRule
        uses: crytic/echidna-action@v2
        with:
          files: test/echidna/EchidnaMaxValueRule.sol
          contract: EchidnaMaxValueRule
          config: echidna.yaml
          echidna-workdir: contracts
          crytic-args: --ignore-compile

      - name: Run Echidna – SpendLimitRule
        uses: crytic/echidna-action@v2
        with:
          files: test/echidna/EchidnaSpendLimitRule.sol
          contract: EchidnaSpendLimitRule
          config: echidna.yaml
          echidna-workdir: contracts
          crytic-args: --ignore-compile
